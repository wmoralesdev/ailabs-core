# Neon Postgres connection string (pooled URL is fine with the Neon adapter)
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DB?sslmode=require"

# Optional: Mistral API key for ticket OCR on /r/$code/new
MISTRAL_API_KEY=""
